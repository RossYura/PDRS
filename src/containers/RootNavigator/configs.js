import colors from 'styles/colors';

export const defaultBottomTabNavigatorConfig = {
  tabBarOptions: {
    showLabel: false,
    activeTintColor: '#ffffff',
    inactiveTintColor: colors._gray,
    style: {
      backgroundColor: '#ffffff',
      borderTopWidth: 1,
      borderTopColor: colors._lightgray,
    },
  },
};


